# SSH

> Part 2. Transferring files

Create a symlink to [`setup.sh`](../part-1/setup.sh) from the previous part. The server's credentials are the same.

Create a script `part-2.sh` which completes the task.

1. Copy the file `~/from/email.addr` from the server. The destination path is `~/to/addr.email`
2. Copy the directory `~/from/emails.d` to the server. The destination path is `~/to/addrs`

---

* What happens when you copy `dir` to `server:new_name`?
